/* 2016.10.04 19:45
 * P_168
 */
#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(int argc, char **argv)
{
	// (a): ix not in area of effect
	for (int ix = 0; ix != sz; ++ix) {}
	if (ix != sz) {}

	// (b): should have three segment in for
	int ix;
	for (ix != sz; ++ix) {}

	// (c): infinite loop
	for (int ix = 0; ix != sz; ++x, ++sz) {}

	return 0;
}

